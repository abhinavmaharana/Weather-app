{
  "name": "weather-app",
  "version": "1.0.0",
  "description": "",
  "main": "backend/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node backend/server.js",
    "dev": "nodemon backend/server.js",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=true YARN_PRODUCTION=true && yarn install --prefix frontend && yarn run build --prefix frontend && npm install --prefix frontend && npm run build --prefix frontend"
  },
  "author": "Abhinav Maharana",
  "license": "ISC",
  "dependencies": {
    "apicache": "^1.6.3",
    "axios": "^0.27.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "express-rate-limit": "^6.4.0",
    "mongoose": "^6.4.4",
    "needle": "^3.1.0",
    "node-fetch": "2.0",
    "request": "^2.88.2"
  }
}
